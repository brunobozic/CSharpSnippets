$(window).resize(function () {

               
	$('.table').css('width', parseInt($('#outerDiv').css("width")) - 50); // sets the table (datatables plugin) width to always be within the boundaries of the container

           
});  
          
$(window).load(function () {

               
	$('.table').css('width', parseInt($('#outerDiv').css("width")) - 50); // sets the table (datatables plugin) width to always be within the boundaries of the container

           
});